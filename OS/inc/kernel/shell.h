#pragma once

/**
 * task_shell - main interactive shell task_shell
 *
 * Handles terminal command input, Janus integration,
 * filesystem commands, scripting, and program launching.
 */
void task_shell(void);
